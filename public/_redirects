
# Netlify redirects file - Static files FIRST
/sitemap.xml /sitemap.xml 200
/sitemap-production.xml /sitemap-production.xml 200
/robots.txt /robots.txt 200

# SPA fallback for everything else - LAST
/* /index.html 200
