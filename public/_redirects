
# NETLIFY REDIRECTS - CRITICAL STATIC FILE PRIORITY

# Force static XML files to be served directly - HIGHEST PRIORITY
/sitemap.xml /sitemap.xml 200!
/sitemap-production.xml /sitemap-production.xml 200!
/robots.txt /robots.txt 200!

# Block SPA routing for static files explicitly
/sitemap* /sitemap.xml 200!
/robots* /robots.txt 200!

# All other routes go to SPA - MUST BE LAST
/* /index.html 200
